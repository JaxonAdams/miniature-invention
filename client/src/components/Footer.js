import React from 'react';

const Footer = () => {
    return (
        <footer>
            <a href='#' className='registry-link'>View Paige's Registry</a>
        </footer>
    );
};

export default Footer;